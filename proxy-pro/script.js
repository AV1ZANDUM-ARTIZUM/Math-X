const input=document.querySelector('#url');const go=document.querySelector('#go');const clear=document.querySelector('#clear');const viewer=document.querySelector('#viewer');const textViewer=document.querySelector('#textViewer');const message=document.querySelector('#message');const modeText=document.querySelector('#modeText');let current='';
function normalize(value){let v=value.trim();if(!v)return '';if(!/^https?:\/\//i.test(v))v='https://'+v;try{const u=new URL(v);if(!['http:','https:'].includes(u.protocol))throw 0;return u.href}catch{return ''}}
function setMessage(t){message.textContent=t}
function direct(url){current=url;textViewer.hidden=true;viewer.hidden=false;viewer.src=url;modeText.textContent='Direct iframe mode · the destination may block embedding.';setMessage('Trying to load '+new URL(url).hostname+' directly…')}
async function reader(){if(!current){setMessage('Enter a URL first.');return}textViewer.hidden=false;viewer.hidden=true;modeText.textContent='Reader mode · fetched through a public text-reading service.';textViewer.textContent='Loading…';setMessage('Fetching readable page text…');try{const r=await fetch('https://r.jina.ai/'+current);if(!r.ok)throw new Error('HTTP '+r.status);const text=await r.text();textViewer.textContent=text||'No readable text returned.';setMessage('Reader loaded.')}catch(e){textViewer.textContent='Reader mode could not fetch this page. Try Open tab or Direct mode.';setMessage('Reader request failed: '+e.message)}}
function start(){const url=normalize(input.value);if(!url){setMessage('Please enter a valid http:// or https:// URL.');return}input.value=url;direct(url)}
go.onclick=start;input.addEventListener('keydown',e=>{if(e.key==='Enter')start()});clear.onclick=()=>{input.value='';current='';viewer.src='about:blank';textViewer.hidden=true;viewer.hidden=false;modeText.textContent='Choose a URL above.';setMessage('')};document.querySelector('#reader').onclick=reader;document.querySelector('#open').onclick=()=>{if(current)window.open(current,'_blank','noopener,noreferrer');else setMessage('Enter a URL first.')};document.querySelector('#fullscreen').onclick=()=>{const el=document.querySelector('.panel');if(document.fullscreenElement)document.exitFullscreen();else el.requestFullscreen?.()};document.querySelectorAll('[data-url]').forEach(b=>b.onclick=()=>{input.value=b.dataset.url;start()});
